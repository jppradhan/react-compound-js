Pills example:

```jsx
<Pills label='One' onRemove={() => {}} />
<Pills label='Two' onRemove={() => {}} />
<Pills label='Three' onRemove={() => {}} />
<Pills label='Four' onRemove={() => {}} />
<Pills label='Five' onRemove={() => {}} />
```
